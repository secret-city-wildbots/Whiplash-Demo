package frc.robot.Utils;

/**
 * An enum containing either a TalonFX, SparkMax, or None type. Used in motor module for determining libraries used
 */
public enum MotorType {
    TFX,
    SPX,
    None,
}