package frc.robot.Utils;

/**
 * An enum containing either a Clockwise direction or Counterclockwise direction. Used for motor module
 */
public enum RotationDir {
    Clockwise,
    CounterClockwise,
}